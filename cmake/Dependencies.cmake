function(bsim_setup_dependencies)

    # No third-party runtime dependencies are required yet.
    #
    # Future dependencies will be configured here so that dependency
    # management remains separate from the main build configuration.

endfunction()
